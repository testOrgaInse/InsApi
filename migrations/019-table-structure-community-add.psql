-- up:

CREATE TABLE IF NOT EXISTS structure_community_add (
    id                serial primary key,
    structure         int REFERENCES structures(id) ON DELETE CASCADE,
    community         int REFERENCES community(id) ON DELETE CASCADE
);

-- down:

DROP TABLE structure_community_add;

-- check:

SELECT EXISTS(
    SELECT *
    FROM information_schema.tables
    WHERE
        table_schema = 'public' AND
        table_name = 'structure_community_add'
);

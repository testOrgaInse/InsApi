-- up:

CREATE TABLE IF NOT EXISTS structure_institute_add (
    id                serial primary key,
    structure         int REFERENCES structures(id) ON DELETE CASCADE,
    institute         int REFERENCES institute(id) ON DELETE CASCADE
);

-- down:

DROP TABLE structure_institute_add;

-- check:

SELECT EXISTS(
    SELECT *
    FROM information_schema.tables
    WHERE
        table_schema = 'public' AND
        table_name = 'structure_institute_add'
);

-- up:

CREATE TABLE IF NOT EXISTS struct_team_account (

);

-- down:

DROP TABLE struct_team_account;

-- check:

SELECT EXISTS(
    SELECT *
    FROM information_schema.tables
    WHERE
        table_schema = 'public' AND
        table_name = 'struct_team_account'
);
